# Kiron Light Playground

Inline code like `console.log("hello\nworld")` is useful for quick checks.

```ts
const message = "preview\tready";
console.info(message);
```

- Check headings
- Check emphasis
- Check code fences
