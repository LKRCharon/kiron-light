# Playground

This folder is for local theme preview only.

Open these files in VS Code with `Kiron Light` active to check token colors across languages.

This folder is excluded from VSIX packaging.
